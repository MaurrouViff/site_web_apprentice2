<header>
    <nav class="flex items-center justify-center min-h-16 bg-gray-200">
        <ul class="flex items-center justify-between lg:gap-4 gap-2">
            <li>
                <a href="{{ path('app_home') }}" class="rounded-md bg-white lg:py-2 lg:px-4 py-1 px-2 outline outline-2 outline-offset-0 outline-black hover:bg-black hover:text-white duration-300 ease-in-out">Accueil</a>
            </li>
            <li>
                <a href="{{ path('app_contact') }}" class="rounded-md bg-white lg:py-2 lg:px-4 py-1 px-2 outline outline-2 outline-offset-0 outline-black hover:bg-black hover:text-white duration-300 ease-in-out">Contact</a>
            </li>
            <li>
                <a href="{{ path('app_magasin') }}" class="rounded-md bg-white lg:py-2 lg:px-4 py-1 px-2 outline outline-2 outline-offset-0 outline-black hover:bg-black hover:text-white duration-300 ease-in-out">Magasin</a>
            </li>
            {% if app.user is defined and app.user %}
                <li>
                    <a href="{{ path('app_mon_compte') }}" class="rounded-md bg-white lg:py-2 lg:px-4 py-1 px-2 outline outline-2 outline-offset-0 outline-black hover:bg-black hover:text-white duration-300 ease-in-out">Mon Compte</a>
                </li>
            {% else %}
                <li>
                    <a href="{{ path('app_login') }}" class="rounded-md bg-white lg:py-2 lg:px-4 py-1 px-2 outline outline-2 outline-offset-0 outline-black hover:bg-black hover:text-white duration-300 ease-in-out">Se connecter</a>
                </li>
            {% endif %}
        </ul>
    </nav>
</header>